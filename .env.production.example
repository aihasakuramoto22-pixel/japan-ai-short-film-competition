# 本番環境用の環境変数サンプル
# このファイルをコピーして .env.production を作成してください

# フロントエンド環境変数（Vite用）
# Renderの静的サイトで設定する場合はこれらを使用
VITE_API_URL=https://api.yoursite.com/api
VITE_STRIPE_PUBLISHABLE_KEY=pk_live_your_stripe_publishable_key

# バックエンド環境変数（Renderのウェブサービスで設定）
# これらはRenderのダッシュボードで直接設定します

# Server Configuration
PORT=3001
FRONTEND_URL=https://yoursite.com

# Cloudflare R2 Configuration (500MB対応)
# R2バケットの作成: https://dash.cloudflare.com/ → R2 → Create bucket
# API Tokenの作成: R2 → Manage R2 API Tokens → Create API token
R2_ENDPOINT=https://<account_id>.r2.cloudflarestorage.com
R2_ACCESS_KEY_ID=your_r2_access_key_id
R2_SECRET_ACCESS_KEY=your_r2_secret_access_key
R2_BUCKET_NAME=japan-ai-film-competition

# Stripe Configuration (本番環境 - クレジット、Apple Pay、Google Pay対応)
STRIPE_SECRET_KEY=sk_live_your_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=pk_live_your_stripe_publishable_key

# Email Configuration (Gmail)
EMAIL_USER=your_email@gmail.com
EMAIL_PASSWORD=your_gmail_app_password

# Optional: Database URL (推奨)
# DATABASE_URL=mongodb+srv://username:password@cluster.mongodb.net/jaisfc
